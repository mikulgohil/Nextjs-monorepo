{
  "buildCommand": "cd ../.. && pnpm run build --filter focus",
  "outputDirectory": "apps/focus/.next",
  "ignoreCommand": "if git rev-parse --verify HEAD^ >/dev/null 2>&1; then git diff --quiet HEAD^ HEAD ./apps/focus/; else exit 1; fi",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
